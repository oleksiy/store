<h1>Products</h1>

<%= link_to "New Product", product_new_path %>

<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product_show_path(product.id) %> 
    </div>
  <% end %>
</div>

